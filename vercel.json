{
  "version": 2,
  "routes": [
    {
      "src": "/",
      "dest": "/api/(.*)"
    },
    {
        "src": "/api/(.*)",
        "methods": [
            "POST",
            "GET"
        ],
        "dest": "/api/(.*)"
    }
  ]
}
